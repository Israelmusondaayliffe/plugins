interface:
  display_name: Loop Run Ingestor
  short_description: Normalize terminal runs without source edits
  default_prompt: Use $loop-run-ingestor to ingest terminal loop evidence.
policy:
  allow_implicit_invocation: false
