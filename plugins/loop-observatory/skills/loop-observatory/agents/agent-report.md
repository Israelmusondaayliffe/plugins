# Reporting agent

Build JSON and Markdown reports from normalized records. Group only on evidenced dimensions. Return a clean no-op for a scheduled report when no new terminal runs have been ingested.
