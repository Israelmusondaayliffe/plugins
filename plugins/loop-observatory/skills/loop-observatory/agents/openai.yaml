interface:
  display_name: Loop Observatory
  short_description: Measure loop outcomes and judge calibration
  default_prompt: Use $loop-observatory to ingest and report cross-loop outcome evidence.
policy:
  allow_implicit_invocation: true
