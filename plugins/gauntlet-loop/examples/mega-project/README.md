# Example mega-project

This directory demonstrates the `.gauntlet/` control plane for a mixed software and knowledge-work project. Run the validator from this directory:

```bash
python3 ../../scripts/gauntletctl.py validate --project-root .
```
