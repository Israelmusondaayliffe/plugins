interface:
  display_name: "Harness Verifier"
  short_description: "Verify a harness against fresh evidence."
  default_prompt: "Use $harness-verifier to verify my operating harness."

policy:
  allow_implicit_invocation: false
