interface:
  display_name: "Harness Audit"
  short_description: "Inspect an existing harness without changing it."
  default_prompt: "Use $harness-audit to inspect my current setup."

policy:
  allow_implicit_invocation: false
