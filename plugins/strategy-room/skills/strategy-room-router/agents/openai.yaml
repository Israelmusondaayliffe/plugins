interface:
  display_name: "Strategy Room Router"
  short_description: "Routes pre-commitment decision work to rigorous interview, assumption challen..."
  default_prompt: "$strategy-room-router Use this skill for the request."
policy:
  allow_implicit_invocation: true
