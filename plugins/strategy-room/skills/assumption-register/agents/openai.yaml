interface:
  display_name: "Assumption Register"
  short_description: "Creates and maintains a structured register of assumptions, evidence, confide..."
  default_prompt: "$assumption-register Use this skill for the request."
policy:
  allow_implicit_invocation: false
