interface:
  display_name: "Session Compounder"
  short_description: "Turns sessions into selected permission-safe outputs"
  default_prompt: "Use $session-compounder to inspect these session notes, recommend worthwhile outputs, and wait for my selection."

policy:
  allow_implicit_invocation: false
