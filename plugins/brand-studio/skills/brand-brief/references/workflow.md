# Brand brief controls

A usable brand attribute changes a visible choice. For example, precise can affect grid, spacing, type, and image selection.
Visual principles should be testable across more than one asset.
Must-avoid directions protect against the most likely generic or off-brand defaults.
The brief cites its strategy and reference sources so later reviewers can trace decisions.

