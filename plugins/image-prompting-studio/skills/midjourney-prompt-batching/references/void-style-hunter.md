# Void Style Hunter

This is a user-supplied draft-exploration recipe, not a universal Midjourney default. Keep the source prompt and settings exact unless the user requests a change.

```text
"" --c 8 --exp 10 --draft --sref random --s 300 --ar
```

The aspect-ratio flag is incomplete in the source template. Before submission, append the requested exact ratio or use the ratio already confirmed for this batch. If none is known, obtain it. Never submit the incomplete template or fill it with a guessed ratio while claiming source fidelity.

1. Run the completed void prompt in Draft for the requested count, after verifying the selected model supports the live operation and Relax is available.
2. Inspect rendered styles and their actual codes. The recipe's author reported 25 images per draft run in their observed workflow; read the actual count from the selected current surface rather than promising 25 everywhere.
3. For selected codes, reuse the full resulting prompts in Draft. Preserve code, profile settings, folder and originating job/image identity. This explores a selected style rather than merely repeating the random seed recipe.
4. Curate the rendered results. Assist with discovery or comparison only when requested, and keep recommendations distinct from the user's choices.
5. Vary selected images to HD within the named scope. Track the HD job and parent image separately, then inspect the result.

A user can ask for one stage or the full chain. Finish at that requested stage. Count initial drafts, selected-code drafts and HD actions separately against the operation cap. Queued work waits; uncertainty about acceptance triggers inspection before retry.

Save a reusable preferred recipe only after the user explicitly selects it. The exact input recipe above is supplied working material, not proof that any particular output has been creatively accepted.
