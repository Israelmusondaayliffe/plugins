interface:
  display_name: "Model Evaluation Router"
  short_description: "Routes model evaluation work across planning, benchmark execution or result n..."
  default_prompt: "$model-evaluation-router Use this skill for the request."
policy:
  allow_implicit_invocation: true
